<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/dstyle.css">
  
  <style>
    .search-results-container {
      max-width: 1200px;
      margin: 20px auto;
      padding: 20px;
    }
    
    .search-header {
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      padding: 30px;
      border-radius: 15px;
      margin-bottom: 30px;
      text-align: center;
    }
    
    .search-header h1 {
      margin: 0;
      font-size: 2rem;
      font-weight: 700;
    }
    
    .search-info {
      margin-top: 10px;
      opacity: 0.9;
    }
    
    .search-filters {
      background: white;
      padding: 20px;
      border-radius: 15px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.08);
      margin-bottom: 30px;
    }
    
    .filter-tabs {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
      justify-content: center;
    }
    
    .filter-tab {
      padding: 8px 16px;
      border: 2px solid #e9ecef;
      border-radius: 25px;
      text-decoration: none;
      color: #6c757d;
      font-weight: 500;
      transition: all 0.3s ease;
    }
    
    .filter-tab.active {
      background: #667eea;
      color: white;
      border-color: #667eea;
    }
    
    .filter-tab:hover {
      border-color: #667eea;
      color: #667eea;
    }
    
    .results-section {
      background: white;
      border-radius: 15px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.08);
      margin-bottom: 30px;
      overflow: hidden;
    }
    
    .results-header {
      background: #f8f9fa;
      padding: 20px 25px;
      border-bottom: 2px solid #e9ecef;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    
    .results-header h3 {
      margin: 0;
      color: #2c3e50;
      font-size: 1.3rem;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    .results-count {
      background: #667eea;
      color: white;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 0.85rem;
    }
    
    .result-item {
      padding: 20px 25px;
      border-bottom: 1px solid #f1f3f4;
      transition: background-color 0.2s ease;
      cursor: pointer;
    }
    
    .result-item:hover {
      background: #f8f9fa;
    }
    
    .result-item:last-child {
      border-bottom: none;
    }
    
    .result-content {
      display: flex;
      align-items: flex-start;
      gap: 15px;
    }
    
    .result-icon {
      width: 45px;
      height: 45px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
      color: white;
      flex-shrink: 0;
    }
    
    .result-icon.facility {
      background: #667eea;
    }
    
    .result-icon.booking {
      background: #00b894;
    }
    
    .result-icon.complaint {
      background: #ff6b6b;
    }
    
    .result-details {
      flex-grow: 1;
    }
    
    .result-title {
      font-weight: 600;
      color: #2c3e50;
      margin-bottom: 5px;
      font-size: 1.1rem;
    }
    
    .result-subtitle {
      color: #6c757d;
      font-size: 0.9rem;
      margin-bottom: 8px;
    }
    
    .result-description {
      color: #7f8c8d;
      line-height: 1.5;
      margin-bottom: 10px;
    }
    
    .result-meta {
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
    }
    
    .meta-item {
      color: #9ca3af;
      font-size: 0.8rem;
      display: flex;
      align-items: center;
      gap: 5px;
    }
    
    .result-actions {
      display: flex;
      gap: 10px;
      margin-top: 10px;
    }
    
    .btn-sm {
      padding: 6px 12px;
      border: none;
      border-radius: 6px;
      font-size: 0.8rem;
      cursor: pointer;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }
    
    .btn-primary {
      background: #667eea;
      color: white;
    }
    
    .btn-success {
      background: #00b894;
      color: white;
    }
    
    .btn-info {
      background: #74b9ff;
      color: white;
    }
    
    .status-badge {
      padding: 3px 8px;
      border-radius: 12px;
      font-size: 0.7rem;
      font-weight: 600;
      text-transform: capitalize;
    }
    
    .status-approved {
      background: #d4edda;
      color: #155724;
    }
    
    .status-pending {
      background: #fff3cd;
      color: #856404;
    }
    
    .status-rejected {
      background: #f8d7da;
      color: #721c24;
    }
    
    .empty-results {
      text-align: center;
      padding: 60px 20px;
      color: #6c757d;
    }
    
    .empty-results i {
      font-size: 4rem;
      margin-bottom: 20px;
      color: #dee2e6;
    }
    
    .alert {
      padding: 12px 20px;
      border-radius: 8px;
      margin-bottom: 20px;
      font-weight: 500;
    }
    
    .alert-error {
      background: #f8d7da;
      color: #721c24;
      border: 1px solid #f5c6cb;
    }
    
    @media (max-width: 768px) {
      .filter-tabs {
        justify-content: flex-start;
      }
      
      .results-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 10px;
      }
      
      .result-item {
        padding: 15px;
      }
      
      .result-meta {
        flex-direction: column;
        gap: 5px;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/userHeader') %>

  <main class="main-content">
    <div class="search-results-container">
      
      <!-- Search Header -->
      <div class="search-header">
        <h1><i class="fas fa-search"></i> Search Results</h1>
        <div class="search-info">
          <% if (searchTerm) { %>
            <p>Showing results for "<strong><%= searchTerm %></strong>" • <%= totalResults %> results found</p>
          <% } else { %>
            <p>Enter a search term to find facilities, bookings, and more</p>
          <% } %>
        </div>
      </div>

      <!-- Error Message -->
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-error">
          <i class="fas fa-exclamation-circle"></i> <%= error %>
        </div>
      <% } %>

      <!-- Search Filters -->
      <div class="search-filters">
        <div class="filter-tabs">
          <a href="/user/search?q=<%= encodeURIComponent(searchTerm || '') %>&type=all" 
             class="filter-tab <%= searchType === 'all' ? 'active' : '' %>">
            All Results
          </a>
          <a href="/user/search?q=<%= encodeURIComponent(searchTerm || '') %>&type=facilities" 
             class="filter-tab <%= searchType === 'facilities' ? 'active' : '' %>">
            Facilities (<%= facilities.length %>)
          </a>
          <a href="/user/search?q=<%= encodeURIComponent(searchTerm || '') %>&type=bookings" 
             class="filter-tab <%= searchType === 'bookings' ? 'active' : '' %>">
            My Bookings (<%= bookings.length %>)
          </a>
          <a href="/user/search?q=<%= encodeURIComponent(searchTerm || '') %>&type=complaints" 
             class="filter-tab <%= searchType === 'complaints' ? 'active' : '' %>">
            Complaints (<%= complaints.length %>)
          </a>
        </div>
      </div>

      <% if (totalResults > 0) { %>
        <!-- Facilities Results -->
        <% if (facilities.length > 0) { %>
          <div class="results-section">
            <div class="results-header">
              <h3>
                <i class="fas fa-building"></i> Facilities
              </h3>
              <span class="results-count"><%= facilities.length %> found</span>
            </div>
            
            <% facilities.forEach(function(facility) { %>
              <div class="result-item">
                <div class="result-content">
                  <div class="result-icon facility">
                    <i class="fas fa-building"></i>
                  </div>
                  <div class="result-details">
                    <div class="result-title"><%= facility.facility_name %></div>
                    <div class="result-subtitle"><%= facility.facility_type %> • <%= facility.location %></div>
                    <div class="result-description"><%= facility.description || 'No description available' %></div>
                    <div class="result-meta">
                      <div class="meta-item">
                        <i class="fas fa-users"></i> Capacity: <%= facility.capacity %>
                      </div>
                      <div class="meta-item">
                        <i class="fas fa-circle"></i> 
                        <span class="<%= facility.status === 'active' ? 'text-success' : 'text-danger' %>">
                          <%= facility.status %>
                        </span>
                      </div>
                    </div>
                    <div class="result-actions">
                      <a href="/user/booking?facility=<%= facility.facility_name %>" class="btn-sm btn-primary">
                        <i class="fas fa-calendar-plus"></i> Book Now
                      </a>
                      <a href="/user/facilities#<%= facility._id %>" class="btn-sm btn-info">
                        <i class="fas fa-eye"></i> View Details
                      </a>
                    </div>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
        <% } %>

        <!-- Bookings Results -->
        <% if (bookings.length > 0) { %>
          <div class="results-section">
            <div class="results-header">
              <h3>
                <i class="fas fa-calendar-check"></i> My Bookings
              </h3>
              <span class="results-count"><%= bookings.length %> found</span>
            </div>
            
            <% bookings.forEach(function(booking) { %>
              <div class="result-item">
                <div class="result-content">
                  <div class="result-icon booking">
                    <i class="fas fa-calendar-check"></i>
                  </div>
                  <div class="result-details">
                    <div class="result-title">
                      <%= booking.facility ? booking.facility.facility_name : 'Unknown Facility' %>
                    </div>
                    <div class="result-subtitle">
                      <%= new Date(booking.date).toLocaleDateString() %> • 
                      <%= booking.startTime %> - <%= booking.endTime %>
                    </div>
                    <div class="result-description"><%= booking.purpose || 'No purpose specified' %></div>
                    <div class="result-meta">
                      <div class="meta-item">
                        <i class="fas fa-clock"></i> 
                        Booked on <%= new Date(booking.createdAt).toLocaleDateString() %>
                      </div>
                      <div class="meta-item">
                        <span class="status-badge status-<%= booking.status.toLowerCase() %>">
                          <%= booking.status %>
                        </span>
                      </div>
                    </div>
                    <div class="result-actions">
                      <a href="/user/bookings" class="btn-sm btn-info">
                        <i class="fas fa-eye"></i> View All Bookings
                      </a>
                    </div>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
        <% } %>

        <!-- Complaints Results -->
        <% if (complaints.length > 0) { %>
          <div class="results-section">
            <div class="results-header">
              <h3>
                <i class="fas fa-exclamation-circle"></i> Complaints
              </h3>
              <span class="results-count"><%= complaints.length %> found</span>
            </div>
            
            <% complaints.forEach(function(complaint) { %>
              <div class="result-item">
                <div class="result-content">
                  <div class="result-icon complaint">
                    <i class="fas fa-exclamation-circle"></i>
                  </div>
                  <div class="result-details">
                    <div class="result-title"><%= complaint.subject %></div>
                    <div class="result-subtitle"><%= complaint.category %> Complaint</div>
                    <div class="result-description">
                      <%= complaint.description.substring(0, 100) %>...
                    </div>
                    <div class="result-meta">
                      <div class="meta-item">
                        <i class="fas fa-calendar"></i> 
                        <%= new Date(complaint.createdAt).toLocaleDateString() %>
                      </div>
                      <div class="meta-item">
                        <span class="status-badge status-<%= complaint.status.toLowerCase().replace(' ', '-') %>">
                          <%= complaint.status %>
                        </span>
                      </div>
                    </div>
                    <div class="result-actions">
                      <a href="/user/complaints/<%= complaint._id %>" class="btn-sm btn-info">
                        <i class="fas fa-eye"></i> View Details
                      </a>
                      <a href="/user/complaints" class="btn-sm btn-primary">
                        <i class="fas fa-list"></i> All Complaints
                      </a>
                    </div>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
        <% } %>

      <% } else { %>
        <!-- Empty Results -->
        <div class="results-section">
          <div class="empty-results">
            <i class="fas fa-search"></i>
            <h3>No results found</h3>
            <% if (searchTerm) { %>
              <p>No results found for "<strong><%= searchTerm %></strong>"</p>
              <p>Try searching with different keywords or check your spelling.</p>
            <% } else { %>
              <p>Enter a search term to find facilities, bookings, complaints, and more.</p>
            <% } %>
          </div>
        </div>
      <% } %>
    </div>
  </main>
</body>
</html>
