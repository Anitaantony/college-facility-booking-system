<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard - EduNexus</title>
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/dstyle.css" />
  
  <style>
    .admin-dashboard {
      max-width: 1400px;
      margin: 20px auto;
      padding: 20px;
    }
    
    .admin-header {
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      padding: 30px;
      border-radius: 15px;
      margin-bottom: 30px;
      text-align: center;
    }
    
    .admin-header h1 {
      margin: 0;
      font-size: 2.8rem;
      font-weight: 700;
    }
    
    .admin-header p {
      margin: 10px 0 0 0;
      font-size: 1.3rem;
      opacity: 0.9;
    }
    
    .stats-overview {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 25px;
      margin-bottom: 40px;
    }
    
    .admin-stat-card {
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
      text-align: center;
      border-top: 5px solid;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      position: relative;
      overflow: hidden;
    }
    
    .admin-stat-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 35px rgba(0,0,0,0.15);
    }
    
    .admin-stat-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 5px;
      background: linear-gradient(90deg, var(--card-color), var(--card-color-light));
    }
    
    .admin-stat-card.total-bookings { --card-color: #74b9ff; --card-color-light: #a29bfe; }
    .admin-stat-card.pending-bookings { --card-color: #fdcb6e; --card-color-light: #f39c12; }
    .admin-stat-card.total-users { --card-color: #00b894; --card-color-light: #00a085; }
    .admin-stat-card.total-facilities { --card-color: #fd79a8; --card-color-light: #e84393; }
    .admin-stat-card.approved-bookings { --card-color: #55a3ff; --card-color-light: #74b9ff; }
    .admin-stat-card.rejected-bookings { --card-color: #ff6b6b; --card-color-light: #ee5a52; }
    
    .stat-icon {
      font-size: 4rem;
      margin-bottom: 20px;
      opacity: 0.8;
    }
    
    .total-bookings .stat-icon { color: #74b9ff; }
    .pending-bookings .stat-icon { color: #fdcb6e; }
    .total-users .stat-icon { color: #00b894; }
    .total-facilities .stat-icon { color: #fd79a8; }
    .approved-bookings .stat-icon { color: #55a3ff; }
    .rejected-bookings .stat-icon { color: #ff6b6b; }
    
    .stat-number {
      font-size: 3rem;
      font-weight: 800;
      margin-bottom: 10px;
      color: #2c3e50;
    }
    
    .stat-label {
      color: #7f8c8d;
      font-size: 1.1rem;
      font-weight: 600;
      margin-bottom: 5px;
    }
    
    .stat-change {
      font-size: 0.9rem;
      font-weight: 600;
      padding: 3px 8px;
      border-radius: 12px;
      margin-top: 8px;
      display: inline-block;
    }
    
    .stat-change.positive {
      background: #d4edda;
      color: #155724;
    }
    
    .stat-change.negative {
      background: #f8d7da;
      color: #721c24;
    }
    
    .admin-sections {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
      gap: 30px;
      margin-bottom: 30px;
    }
    
    .admin-section {
      background: white;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.08);
    }
    
    .section-header {
      display: flex;
      justify-content: between;
      align-items: center;
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 2px solid #f8f9fa;
    }
    
    .section-title {
      font-size: 1.4rem;
      font-weight: 700;
      color: #2c3e50;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    .section-title i {
      color: #667eea;
    }
    
    .quick-actions-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
    }
    
    .quick-action-btn {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 15px;
      background: #f8f9fa;
      border: none;
      border-radius: 10px;
      text-decoration: none;
      color: #495057;
      font-weight: 600;
      transition: all 0.3s ease;
      cursor: pointer;
    }
    
    .quick-action-btn:hover {
      background: #667eea;
      color: white;
      text-decoration: none;
      transform: translateY(-2px);
    }
    
    .recent-activity-item {
      display: flex;
      align-items: center;
      padding: 15px 0;
      border-bottom: 1px solid #f8f9fa;
    }
    
    .recent-activity-item:last-child {
      border-bottom: none;
    }
    
    .activity-avatar {
      width: 45px;
      height: 45px;
      border-radius: 50%;
      background: #667eea;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 600;
      margin-right: 15px;
    }
    
    .activity-content {
      flex: 1;
    }
    
    .activity-title {
      font-weight: 600;
      color: #2c3e50;
      margin-bottom: 3px;
    }
    
    .activity-time {
      color: #7f8c8d;
      font-size: 0.9rem;
    }
    
    .activity-status {
      padding: 3px 8px;
      border-radius: 12px;
      font-size: 0.8rem;
      font-weight: 600;
    }
    
    .status-pending {
      background: #fff3cd;
      color: #856404;
    }
    
    .status-approved {
      background: #d4edda;
      color: #155724;
    }
    
    .status-rejected {
      background: #f8d7da;
      color: #721c24;
    }

    /* Management Section Styles */
    .management-categories {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 20px;
    }

    .management-category {
      background: #f8f9fa;
      padding: 20px;
      border-radius: 10px;
      border-left: 4px solid #667eea;
    }

    .management-category h4 {
      color: #2c3e50;
      margin-bottom: 15px;
      font-size: 1.1rem;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .management-links {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .management-link {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 8px 12px;
      background: white;
      border-radius: 6px;
      text-decoration: none;
      color: #495057;
      font-weight: 500;
      transition: all 0.3s ease;
    }

    .management-link:hover {
      background: #667eea;
      color: white;
      text-decoration: none;
      transform: translateX(5px);
    }
    
    @media (max-width: 768px) {
      .stats-overview {
        grid-template-columns: 1fr;
      }
      
      .admin-sections {
        grid-template-columns: 1fr;
      }
      
      .quick-actions-grid {
        grid-template-columns: 1fr;
      }

      .management-categories {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/adminHeader') %>

  <main class="main-content" style="margin-top: 20px;">
    <div class="admin-dashboard">
      
      <!-- Admin Header -->
      <div class="admin-header">
        <h1><i class="fas fa-tachometer-alt"></i> Admin Dashboard</h1>
        <p>EduNexus Facility Management System</p>
      </div>

      <!-- Statistics Overview -->
      <div class="stats-overview">
        <div class="admin-stat-card total-bookings">
          <div class="stat-icon">
            <i class="fas fa-calendar-check"></i>
          </div>
          <div class="stat-number"><%= totalBookings || 0 %></div>
          <div class="stat-label">Total Bookings</div>
          <div class="stat-change positive">
            <i class="fas fa-arrow-up"></i> +12% from last month
          </div>
        </div>
        
        <div class="admin-stat-card pending-bookings">
          <div class="stat-icon">
            <i class="fas fa-clock"></i>
          </div>
          <div class="stat-number"><%= pendingBookings || 0 %></div>
          <div class="stat-label">Pending Approval</div>
          <div class="stat-change negative">
            <i class="fas fa-exclamation-triangle"></i> Needs attention
          </div>
        </div>
        
        <div class="admin-stat-card approved-bookings">
          <div class="stat-icon">
            <i class="fas fa-check-circle"></i>
          </div>
          <div class="stat-number"><%= approvedBookings || 0 %></div>
          <div class="stat-label">Approved Bookings</div>
          <div class="stat-change positive">
            <i class="fas fa-arrow-up"></i> +8% this week
          </div>
        </div>
        
        <div class="admin-stat-card total-users">
          <div class="stat-icon">
            <i class="fas fa-users"></i>
          </div>
          <div class="stat-number"><%= totalUsers || 0 %></div>
          <div class="stat-label">Total Users</div>
          <div class="stat-change positive">
            <i class="fas fa-arrow-up"></i> +5 new users
          </div>
        </div>
        
        <div class="admin-stat-card total-facilities">
          <div class="stat-icon">
            <i class="fas fa-building"></i>
          </div>
          <div class="stat-number"><%= totalFacilities || 0 %></div>
          <div class="stat-label">Total Facilities</div>
          <div class="stat-change positive">
            <i class="fas fa-arrow-up"></i> All active
          </div>
        </div>
        
        <div class="admin-stat-card rejected-bookings">
          <div class="stat-icon">
            <i class="fas fa-times-circle"></i>
          </div>
          <div class="stat-number"><%= rejectedBookings || 0 %></div>
          <div class="stat-label">Rejected Bookings</div>
          <div class="stat-change negative">
            <i class="fas fa-arrow-down"></i> -3% this month
          </div>
        </div>
      </div>

      <!-- Admin Sections -->
      <div class="admin-sections">
        <!-- Quick Actions -->
        <div class="admin-section">
          <div class="section-header">
            <h3 class="section-title">
              <i class="fas fa-bolt"></i> Quick Actions
            </h3>
          </div>
          
          <div class="quick-actions-grid">
            <a href="/admin/manageBookings" class="quick-action-btn">
              <i class="fas fa-calendar-alt"></i>
              Manage Bookings
            </a>
            <a href="/admin/manageFacilities" class="quick-action-btn">
              <i class="fas fa-building"></i>
              Manage Facilities
            </a>
            <a href="/admin/manageProfile" class="quick-action-btn">
              <i class="fas fa-users-cog"></i>
              Manage Users
            </a>
            <a href="/admin/viewComplaints" class="quick-action-btn">
              <i class="fas fa-exclamation-circle"></i>
              View Complaints
            </a>
          </div>
        </div>

        
        <!-- Recent Activities -->
        <div class="admin-section">
          <div class="section-header">
            <h3 class="section-title">
              <i class="fas fa-history"></i> Recent Activities
            </h3>
          </div>
          
          <div class="recent-activities">
            <% if (recentBookings && recentBookings.length > 0) { %>
              <% recentBookings.forEach(function(booking) { %>
                <div class="recent-activity-item">
                  <div class="activity-avatar">
                    <%= booking.user.user_name.charAt(0).toUpperCase() %>
                  </div>
                  <div class="activity-content">
                    <div class="activity-title">
                      <%= booking.user.user_name %> booked <%= booking.facility.facility_name %>
                    </div>
                    <div class="activity-time">
                      <%= booking.createdAt.toDateString() %> • <%= booking.date.toDateString() %>
                    </div>
                  </div>
                  <div class="activity-status status-<%= booking.status.toLowerCase() %>">
                    <%= booking.status %>
                  </div>
                </div>
              <% }); %>
            <% } else { %>
              <div class="recent-activity-item">
                <div class="activity-avatar">
                  <i class="fas fa-info-circle"></i>
                </div>
                <div class="activity-content">
                  <div class="activity-title">No recent activities</div>
                  <div class="activity-time">System is ready for bookings</div>
                </div>
              </div>
            <% } %>
          </div>
        </div>

        <!-- System Status -->
        <div class="admin-section">
          <div class="section-header">
            <h3 class="section-title">
              <i class="fas fa-server"></i> System Status
            </h3>
          </div>
          
          <div class="system-status">
            <div class="recent-activity-item">
              <div class="activity-avatar" style="background: #00b894;">
                <i class="fas fa-database"></i>
              </div>
              <div class="activity-content">
                <div class="activity-title">Database Connection</div>
                <div class="activity-time">All systems operational</div>
              </div>
              <div class="activity-status status-approved">Online</div>
            </div>
            
            <div class="recent-activity-item">
              <div class="activity-avatar" style="background: #74b9ff;">
                <i class="fas fa-cloud"></i>
              </div>
              <div class="activity-content">
                <div class="activity-title">Server Status</div>
                <div class="activity-time">Last updated: Just now</div>
              </div>
              <div class="activity-status status-approved">Running</div>
            </div>
            
            <div class="recent-activity-item">
              <div class="activity-avatar" style="background: #fdcb6e;">
                <i class="fas fa-shield-alt"></i>
              </div>
              <div class="activity-content">
                <div class="activity-title">Security Status</div>
                <div class="activity-time">All security checks passed</div>
              </div>
              <div class="activity-status status-approved">Secure</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
